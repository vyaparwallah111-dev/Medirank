import { MapPin, Search, Stethoscope } from 'lucide-react';
type KnowledgeBase={area_name:string;city_name:string;top_services:string[]};
export function KnowledgeBaseSettings({initial,specialization}:{initial:KnowledgeBase;specialization:string}){return <section className="card mt-6 p-5 sm:p-7"><div className="flex gap-3"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-orange-50 text-orange"><Search size={20}/></span><div><h2 className="font-bold">Knowledge Base</h2><p className="mt-1 text-sm leading-6 text-slate-500">Add accurate location and treatment details once. They help personalise patient prompts and locally relevant reviews.</p></div></div><div className="mt-6 grid gap-5 sm:grid-cols-2"><div><label className="label">Area / locality</label><div className="relative"><MapPin className="absolute left-3 top-3.5 text-slate-400" size={18}/><input name="area_name" className="input pl-10" defaultValue={initial.area_name} placeholder="Kankarbagh" maxLength={80}/></div></div><div><label className="label">City</label><div className="relative"><MapPin className="absolute left-3 top-3.5 text-slate-400" size={18}/><input name="city_name" className="input pl-10" defaultValue={initial.city_name} placeholder="Patna" maxLength={80}/></div></div><div className="sm:col-span-2"><label className="label">Specialisation</label><div className="flex min-h-12 items-center gap-3 rounded-xl border border-slate-200 bg-slate-50 px-4 text-sm text-slate-600"><Stethoscope size={18}/>{specialization||'Not specified'}<span className="ml-auto text-xs text-slate-400">Edit above</span></div></div><div className="sm:col-span-2"><label className="label">Top treatments / services</label><textarea name="top_services" className="input min-h-28 resize-y" defaultValue={initial.top_services.join(', ')} placeholder="root canal, teeth cleaning, braces, cavity filling" maxLength={600}/><p className="mt-2 text-xs text-slate-400">Separate services with commas. Add up to 12 specific treatments patients actually receive.</p></div></div></section>}
