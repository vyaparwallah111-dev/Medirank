'use client';
import { useState } from 'react'; import { Check, Palette } from 'lucide-react';
type Theme={primary:string;accent:string;background:string};
const palettes=[{name:'Medical trust',primary:'#1E40AF',accent:'#F97316',background:'#F8FAFC'},{name:'Fresh care',primary:'#047857',accent:'#14B8A6',background:'#F0FDFA'},{name:'Calm violet',primary:'#6D28D9',accent:'#EC4899',background:'#FAF5FF'},{name:'Premium navy',primary:'#172554',accent:'#D97706',background:'#FFFBEB'}];
export function ThemePicker({initial}:{initial:Theme}){const [theme,setTheme]=useState(initial);return <section className="card mt-6 p-5 sm:p-7"><div className="flex gap-3"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-xl bg-blue-50 text-brand"><Palette size={20}/></span><div><h2 className="font-bold">Patient Page Theme Settings</h2><p className="mt-1 text-sm text-slate-500">Match the public review experience to your clinic brand.</p></div></div><div className="mt-6 grid gap-3 sm:grid-cols-2">{palettes.map(p=>{const active=p.primary===theme.primary&&p.accent===theme.accent&&p.background===theme.background;return <button type="button" key={p.name} onClick={()=>setTheme(p)} className={`flex min-h-16 items-center gap-3 rounded-xl border p-3 text-left ${active?'border-brand ring-2 ring-blue-100':'border-slate-200'}`}><span className="flex -space-x-2"><i className="h-8 w-8 rounded-full border-2 border-white" style={{background:p.primary}}/><i className="h-8 w-8 rounded-full border-2 border-white" style={{background:p.accent}}/></span><span className="flex-1 text-sm font-semibold">{p.name}</span>{active&&<Check size={17} className="text-brand"/>}</button>})}</div><div className="mt-6 grid gap-4 sm:grid-cols-3">{([['Primary','primary'],['Accent','accent'],['Background','background']] as const).map(([label,key])=><label key={key} className="text-sm font-medium text-slate-700">{label}<span className="mt-2 flex items-center gap-2 rounded-xl border border-slate-200 p-2"><input type="color" value={theme[key]} onChange={e=>setTheme({...theme,[key]:e.target.value.toUpperCase()})} className="h-9 w-10 cursor-pointer border-0 bg-transparent"/><input name={`theme_${key}`} value={theme[key]} onChange={e=>setTheme({...theme,[key]:e.target.value})} pattern="#[0-9A-Fa-f]{6}" className="min-w-0 flex-1 bg-transparent font-mono text-xs outline-none"/></span></label>)}</div><div className="mt-5 rounded-xl p-4" style={{background:theme.background}}><p className="text-xs font-semibold uppercase tracking-wider" style={{color:theme.primary}}>Live preview</p><button type="button" className="mt-2 min-h-11 rounded-lg px-4 text-sm font-bold text-white" style={{background:theme.primary}}>Generate my review</button><span className="ml-3 text-sm font-bold" style={{color:theme.accent}}>★★★★★</span></div></section>}
