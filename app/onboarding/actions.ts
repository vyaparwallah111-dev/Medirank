'use server';
import { redirect } from 'next/navigation'; import { getAuthenticatedUser } from '@/lib/dashboard';
const keywordSets:Record<string,{keyword:string;category:string}[]>={dentist:[{keyword:'Painless root canal',category:'treatment'},{keyword:'Best dental implant',category:'treatment'},{keyword:'Teeth whitening',category:'treatment'},{keyword:'Painless tooth extraction',category:'treatment'},{keyword:'Advanced clinic treatment',category:'treatment'},{keyword:'Friendly and caring doctor',category:'behaviour'},{keyword:'Explained treatment clearly',category:'behaviour'},{keyword:'Clean and hygienic clinic',category:'cleanliness'}],default:[{keyword:'Advanced treatment',category:'treatment'},{keyword:'Friendly and caring doctor',category:'behaviour'},{keyword:'Explained treatment clearly',category:'behaviour'},{keyword:'Clean and hygienic clinic',category:'cleanliness'},{keyword:'On-time appointment',category:'clinic'}]};
export async function completeOnboarding(formData:FormData){const {supabase,user}=await getAuthenticatedUser();const {data:existing}=await supabase.from('doctors').select('id').eq('auth_user_id',user.id).maybeSingle();if(existing)redirect('/dashboard');const doctorName=String(formData.get('doctor_name')||'').trim(),specialization=String(formData.get('specialization')||'').trim(),base=doctorName.replace(/^dr\.?\s*/i,'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')||'doctor',slug=`${base}-${crypto.randomUUID().slice(0,6)}`;const {data:doctor,error}=await supabase.from('doctors').insert({auth_user_id:user.id,doctor_name:doctorName,clinic_name:String(formData.get('clinic_name')||'').trim(),specialization,city:String(formData.get('city')||'').trim(),gmb_review_link:String(formData.get('gmb_review_link')||'').trim()||null,slug,subscription_tier:'starter'}).select('id').single();if(error||!doctor)throw new Error(error?.message||'Unable to create clinic profile.');const key=Object.keys(keywordSets).find(k=>specialization.toLowerCase().includes(k));const seed=keywordSets[key||'default'];const {error:keywordError}=await supabase.from('doctor_keywords').insert(seed.map(x=>({...x,doctor_id:doctor.id})));if(keywordError)throw new Error(keywordError.message);redirect('/dashboard')}
