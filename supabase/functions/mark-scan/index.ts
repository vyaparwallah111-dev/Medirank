import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type'};
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});try{const {scan_id,event}=await req.json();if(!scan_id||!['generated','copied','posted'].includes(event))return Response.json({error:'Invalid event'},{status:400,headers:cors});const admin=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!);const update=event==='generated'?{review_generated:true}:event==='copied'?{review_copied:true}:{redirected_to_gmb:true};const {error}=await admin.from('scans').update(update).eq('id',scan_id);if(error)throw error;return Response.json({ok:true},{headers:cors})}catch(e){return Response.json({error:e.message},{status:500,headers:cors})}});
