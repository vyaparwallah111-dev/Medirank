import { createServerClient } from '@supabase/ssr'; import { NextResponse, type NextRequest } from 'next/server';
type CookieToSet={name:string;value:string;options?:Record<string,unknown>};
export async function middleware(req:NextRequest){let res=NextResponse.next({request:req});const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY;if(!url||!key)return res;const supabase=createServerClient(url,key,{cookies:{getAll:()=>req.cookies.getAll(),setAll(c:CookieToSet[]){c.forEach(({name,value})=>req.cookies.set(name,value));res=NextResponse.next({request:req});c.forEach(({name,value,options})=>res.cookies.set(name,value,options as any))}}});const {data:{user}}=await supabase.auth.getUser();if(!user&&(req.nextUrl.pathname.startsWith('/dashboard')||req.nextUrl.pathname.startsWith('/admin')))return NextResponse.redirect(new URL('/login',req.url));if(user&&(req.nextUrl.pathname==='/login'||req.nextUrl.pathname==='/signup'))return NextResponse.redirect(new URL('/dashboard',req.url));return res}
export const config={matcher:['/dashboard/:path*','/admin/:path*','/onboarding','/login','/signup']};
